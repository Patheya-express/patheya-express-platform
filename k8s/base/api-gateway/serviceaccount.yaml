# No Role/RoleBinding attached — the app never calls the Kubernetes API, so the least-privilege
# answer is zero grants rather than fabricating permissions it doesn't need.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: api-gateway
  labels:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/component: api
automountServiceAccountToken: false
